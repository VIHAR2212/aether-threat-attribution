# ====================================================================
# Project AETHER - Environment Configuration Template
# Autonomous Engine for Threat Harmonization & Entity Resolution
# NTRO PS-26151
# ====================================================================

# Authentication & Access Control
# Set a strong 32+ character key in staging/production
AETHER_API_KEY=aether-investigator-dev-key-2026
AETHER_REQUIRE_AUTH=true

# CORS Allowed Origins (comma-separated list of permitted web origins)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Environment Mode: development | staging | production
ENVIRONMENT=development

# Database Connection
# Local development default uses SQLite; production should use PostgreSQL
DATABASE_URL=sqlite:///./aether_dev.db
# DATABASE_URL=postgresql+psycopg2://aether_user:CHANGE_ME_STRONG_PASSWORD@127.0.0.1:5432/aether_db

# Request & Transmission Guards
MAX_PAYLOAD_BYTES=2097152
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_INVESTIGATE_PER_MINUTE=10

# External Threat Intelligence APIs (Optional - leave blank if not configured)
SHODAN_API_KEY=
CENSYS_API_ID=
CENSYS_API_SECRET=
TOR_SOCKS_PROXY=socks5h://127.0.0.1:9050

# PostgreSQL Container Credentials (used by docker-compose)
POSTGRES_USER=aether
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD
POSTGRES_DB=aether
